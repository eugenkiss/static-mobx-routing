import * as React from 'react'
import styled from 'styled-components'

const Centered = styled.div`
  position: absolute;
  top: 50%;
  left: 50%;
`

export const NotFound = () => <Centered>Not Found</Centered>
